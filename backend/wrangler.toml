name = "microblog-api"
main = "src/index.ts"
compatibility_date = "2024-11-24"

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "microblog-db"
database_id = "local"  # ローカル開発用、本番デプロイ時に実際のIDに置き換え

# Workers Secrets (ローカル開発用は .dev.vars に記載)
# AUTH_PASSWORD = "your-password-here"
